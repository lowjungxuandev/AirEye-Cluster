apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: aireye-app-secret
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  vaultAuthRef: vault-auth
  mount: secret
  type: kv-v2
  path: aireye-app-secret
  refreshAfter: 30s
  destination:
    name: aireye-app-secret
    create: true
    overwrite: true
  rolloutRestartTargets:
    - kind: Deployment
      name: aireye-app
