apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  annotations:
    argocd.argoproj.io/sync-options: Replace=false
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 40Gi
