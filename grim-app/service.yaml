apiVersion: v1
kind: Service
metadata:
  name: grim-app
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  selector:
    app: grim-app
  ports:
    - name: http
      port: 80
      targetPort: http
